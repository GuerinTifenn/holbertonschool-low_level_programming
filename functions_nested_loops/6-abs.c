#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _abs - check the code
 *@n: caracter
 * Return: Always 0.
 */
int _abs(int n)
{
	if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}
