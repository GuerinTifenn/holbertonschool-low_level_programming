#include <stdio.h>
#include <string.h>
#include "main.h"

/**
  * _strlen - returns the length of a string.
  * @s: a pointer to an int
  * Return: Always 0 (Success)
  */

int _strlen(char *s)
{
	return (strlen(s));
}
